{
  "name": "offeragent",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run typecheck && npm run build:protocol && npm run build:runtime && npm run build:plugin",
    "build:plugin": "tsc -p packages/plugin/tsconfig.build.json && esbuild packages/plugin/src/main.ts --bundle --platform=node --format=cjs --target=node20 --external:obsidian --outfile=packages/plugin/dist/main.js && node scripts/package-plugin.mjs",
    "build:protocol": "tsc -p packages/protocol/tsconfig.build.json",
    "build:runtime": "esbuild packages/runtime/src/cli.ts --bundle --platform=node --format=cjs --target=node20 --outfile=packages/runtime/dist/cli.js",
    "test": "node --test tests/*.test.mjs",
    "test:live-codex": "set OFFERAGENT_LIVE_CODEX=1&& node --test tests/live-codex.test.mjs",
    "test:runtime": "node --test tests/runtime-lifecycle.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.0",
    "typescript": "^5.8.0",
    "ws": "^8.18.0"
  }
}
