# Use additive Pinned Context and show used evidence

OfferAgent 允许用户在发送前固定希望优先考虑的 Vault 文档，但 Pinned Context 不构成读取白名单，Agent 仍可按任务需要搜索其他 Vault 内容。界面在运行前展示固定文档，并在运行中和回答后单独展示实际形成 Evidence Snapshot 的来源路径与范围，从而同时保留 Agent 的检索能力和用户对真实上下文的可见性。
