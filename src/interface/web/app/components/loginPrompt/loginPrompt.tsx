"use client";

export interface LoginPromptProps {
    onOpenChange: (open: boolean) => void;
    isMobileWidth?: boolean;
}

export default function LoginPrompt(_props: LoginPromptProps) {
    return null;
}
